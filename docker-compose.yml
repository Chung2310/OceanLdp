services:
  ocean-ldp:
    image: ${IMAGE_NAME:-ocean-ldp}:${IMAGE_TAG:-latest}
    build:
      context: .
    restart: unless-stopped
    ports:
      - "${PORT:-3010}:80"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
